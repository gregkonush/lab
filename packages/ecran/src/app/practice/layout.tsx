export default function PracticeLayout({ children }: { children: React.ReactNode }) {
  return <div className="min-h-[calc(100vh-10rem)] mt-5 flex flex-col justify-start items-center">{children}</div>
}
