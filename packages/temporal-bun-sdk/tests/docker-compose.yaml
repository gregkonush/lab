version: '3.9'

services:
  temporal:
    image: temporalio/auto-setup:1.23.0
    container_name: temporal-test
    environment:
      - TEMPORAL_DEFAULT_NAMESPACE=default
    ports:
      - '7233:7233'
