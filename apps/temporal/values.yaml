temporal:
  prometheus:
    enabled: false
  grafana:
    enabled: false
  web:
    ingress:
      enabled: true
      className: traefik
      hosts:
        - "temporal.lan"
  server:
    frontend:
      ingress:
        enabled: true
        className: traefik
        hosts:
          - "frontend.temporal.lan"
