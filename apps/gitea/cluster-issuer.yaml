apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-http01
  namespace: gitea
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: gkonushev@outlook.com
    privateKeySecretRef:
      name: letsencrypt-http01-account-key
    solvers:
    - http01:
        ingress:
          class: traefik
