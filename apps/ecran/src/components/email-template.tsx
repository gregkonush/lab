import * as React from 'react'

interface EmailTemplateProps {}

export const EmailTemplate: React.FC<Readonly<EmailTemplateProps>> = () => (
  <div>Welcome, traveller. We will let you know when our app is ready.</div>
)
