---
- name: Disable firewall
  hosts: all
  become: true
  tasks:
    - name: Stop UFW service
      systemd:
        name: ufw
        state: stopped
      ignore_errors: true
    - name: Disable UFW service at boot
      systemd:
        name: ufw
        enabled: false
