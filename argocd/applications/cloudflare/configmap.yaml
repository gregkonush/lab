apiVersion: v1
kind: ConfigMap
metadata:
  name: cloudflared-config
  namespace: cloudflare
data:
  config.yaml: |
    tunnel-token: $(cat /etc/cloudflared/token)
    no-autoupdate: true
    metrics: 0.0.0.0:2000 