apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: dernier-db
spec:
  instances: 1
  primaryUpdateStrategy: unsupervised
  storage:
    size: 30Gi
  bootstrap:
    initdb:
      database: dernier
      owner: dernier
  monitoring:
    enablePodMonitor: true
