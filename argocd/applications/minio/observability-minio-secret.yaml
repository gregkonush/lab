apiVersion: v1
kind: Secret
metadata:
  name: observability-minio-creds
  namespace: minio
type: Opaque
stringData:
  config.env: |
    export MINIO_ROOT_USER="minio-root"
    export MINIO_ROOT_PASSWORD="minio-supersecret"
