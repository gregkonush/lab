apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: registry
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - pvc.yaml
helmCharts:
  - name: docker-registry-ui
    repo: https://helm.joxit.dev
    version: 1.1.3
    releaseName: registry
    namespace: registry
    valuesFile: registry-ui-values.yaml
