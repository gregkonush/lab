---
imagePullSecrets:
  - name: registry

dagsterWebserver:
  image:
    repository: kalmyk.duckdns.org/lab/alchimie
    tag: latest
    pullPolicy: Always

dagster-user-deployments:
  imagePullSecrets:
    - name: registry
  deployments: []

dagsterDaemon:
  image:
    repository: kalmyk.duckdns.org/lab/alchimie
    tag: latest
    pullPolicy: Always
