---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: discord-codex-bot
  namespace: argo-workflows
spec:
  encryptedData:
    bot-token: REPLACE_WITH_SEALED_BOT_TOKEN
    guild-id: REPLACE_WITH_SEALED_GUILD_ID
    category-id: REPLACE_WITH_SEALED_CATEGORY_ID
  template:
    metadata:
      name: discord-codex-bot
      namespace: argo-workflows
    type: Opaque
