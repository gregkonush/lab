apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: rails-latest-secrets
  namespace: rails-latest
spec:
  encryptedData:
    RAILS_MASTER_KEY: REPLACE_WITH_SEALED_VALUE
    SECRET_KEY_BASE: REPLACE_WITH_SEALED_VALUE
  template:
    metadata:
      name: rails-latest-secrets
      namespace: rails-latest
    type: Opaque
