apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: courriel
  namespace: argo-workflows
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames:
      - kafka-codex-credentials
      - courriel-resend
    verbs: ["get"]
