<% content_for :title, "Dernier • Hotwire Demo" %>

<section class="grid gap-8">
  <div class="rounded-2xl border border-slate-800 bg-slate-900/40 p-8 shadow-lg shadow-slate-950/40">
    <h1 class="text-3xl font-semibold tracking-tight text-white">Welcome to Dernier</h1>
    <p class="mt-4 text-base leading-relaxed text-slate-300">
      This page is powered by <span class="font-medium text-sky-300">Hotwire</span>,
      with Turbo handling navigation updates and Stimulus wiring up interactive
      behaviors without a heavy JavaScript bundle.
    </p>
  </div>

  <div class="grid gap-4 md:grid-cols-2">
    <div class="rounded-xl border border-slate-800 bg-slate-900/40 p-6">
      <h2 class="text-xl font-semibold text-white">Live Message</h2>
      <div class="mt-3 rounded-lg border border-slate-800 bg-slate-950/60 p-4 text-lg font-medium text-sky-200" data-controller="hello"></div>
      <p class="mt-2 text-sm text-slate-400">
        The default Stimulus controller replaces the text inside this block when it connects.
      </p>
    </div>

    <turbo-frame id="clock" class="rounded-xl border border-slate-800 bg-slate-900/40 p-6">
      <h2 class="text-xl font-semibold text-white">Turbo Frame</h2>
      <p class="mt-3 text-2xl font-mono text-sky-200">
        <%= Time.current.in_time_zone("UTC").strftime("%Y-%m-%d %H:%M:%SZ") %>
      </p>
      <p class="mt-2 text-sm text-slate-400">
        Turbo frames let parts of the page refresh without a full reload—replace this frame via Turbo to keep the clock current.
      </p>
    </turbo-frame>
  </div>
</section>
