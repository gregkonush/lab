apiVersion: apps/v1
kind: Deployment
metadata:
  name: facteur
  namespace: facteur
spec:
  template:
    spec:
      containers:
        - name: facteur
          env:
            - name: FACTEUR_REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: redis-shared
                  key: url
