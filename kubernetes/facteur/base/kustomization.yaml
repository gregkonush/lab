apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - serviceaccount.yaml
  - workflow-serviceaccount.yaml
  - rolebinding.yaml
  - service.yaml
  - internal-service.yaml
  - workflowtemplate.yaml
  - redis.yaml
  - kafkasource.yaml
  - codex-kafkasource.yaml
configMapGenerator:
  - name: facteur-config
    files:
      - config.yaml
generatorOptions:
  disableNameSuffixHash: true
